# SOURCE: SigmaHQ proc_access_win_lsass_dump_comsvcs_dll.yml
#         mdecrevoisier windows-lsass MiniDump via comsvcs
# CONTEXT: "rundll32 comsvcs.dll MiniDump <lsass_pid> output.dmp full" is a
# built-in Windows technique to dump LSASS without Mimikatz or Procdump.
# It only requires comsvcs.dll which exists on all Windows systems.
# The CommandLine must contain ALL THREE: comsvcs, MiniDump, and a target
# (either lsass.exe reference or the word "full").
# FALSE POSITIVE: None. This specific combination of comsvcs.dll + MiniDump
# + LSASS reference has no legitimate administrative use.
title: LSASS Dump via comsvcs.dll MiniDump (Living-off-the-Land)
event_id: "4688"
event_ids: ["4688", "1"]
description: >
  rundll32.exe was used to call comsvcs.dll MiniDump to dump LSASS memory.
  This is a built-in Windows credential dumping technique requiring only
  comsvcs.dll which exists on all systems. No external tools needed.
  Command: rundll32 C:\Windows\System32\comsvcs.dll MiniDump <PID> file.dmp full
  Used by ransomware affiliates and APT groups to avoid EDR detections
  that focus on Mimikatz/Procdump.
  Source: SigmaHQ proc_access_win_lsass_dump_comsvcs_dll.yml
severity: critical
confidence: 0.97
technique: T1003.001
mitre: Credential Access
conditions:
  - field: CommandLine
    contains_all:
      - "comsvcs"
      - "MiniDump"
  - field: CommandLine
    contains_any:
      - "lsass"
      - " full"
      - ".dmp"
